const os = require('os');
const fs = require('fs');
const path = require('path');
const dotenv = require('dotenv');

// Load .env from project root
const projectRoot = path.resolve(__dirname, '../../server/');
dotenv.config({ path: path.join(projectRoot, '.env') });

// Detect LAN IP
function getLocalIp() {
  const interfaces = os.networkInterfaces();
  for (const name of Object.keys(interfaces)) {
    for (const net of interfaces[name]) {
      if (net.family === 'IPv4' && !net.internal) {
        return net.address;
      }
    }
  }
  return 'localhost';
}

const localIp = getLocalIp();
const port = process.env.APP_PORT || '3000';

const envPath = path.join(__dirname, '../src/environments/environment.local.ts');
const output = `
// 🚨 AUTO-GENERATED by set-local-env.js — do not edit manually.
export const ENVIRONMENT = {
  baseUrl: 'http://${localIp}:${port}',
  env: 'local'
};
`;

fs.writeFileSync(envPath, output.trimStart());
console.log(`✅ Wrote environment.local.ts with IP: ${localIp} and port: ${port}`);
