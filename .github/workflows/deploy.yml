name: Deploy & Release (Web, Desktop, Mobile)

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Build and SonarQube Analysis"]
    types: [completed]
    branches: [main]

permissions:
  contents: write

jobs:
  deploy-web:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "📦 Web App Version: $VERSION"

      - name: Deploy to Heroku
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
        run: |
          echo "🚀 Deploying v$VERSION to Heroku..."
          git remote add heroku https://heroku:$HEROKU_API_KEY@git.heroku.com/angular-momentum.git
          git push heroku main:main
          echo "✅ Deployment complete"

  create-release:
    runs-on: ubuntu-latest
    # if: github.event_name == 'workflow_dispatch' || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    outputs:
      release_id: ${{ steps.create_release.outputs.release_id }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Get version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        id: create_release
        run: |
          # Check if release already exists
          if gh release view v${{ steps.version.outputs.version }} &>/dev/null; then
            echo "Release v${{ steps.version.outputs.version }} already exists, using existing release"
            RELEASE_ID="v${{ steps.version.outputs.version }}"
          else
            echo "Creating new release v${{ steps.version.outputs.version }}"
            gh release create v${{ steps.version.outputs.version }} \
              --title "Release v${{ steps.version.outputs.version }}" \
              --generate-notes
            RELEASE_ID="v${{ steps.version.outputs.version }}"
          fi
          echo "release_id=$RELEASE_ID" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-and-release:
    needs: create-release
    runs-on: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target x86_64-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: ''
          - platform: 'windows-latest'
            args: ''

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.14.0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        run: |
          cd client
          npm ci

      - name: Build & Release with Tauri Action
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

          # Tauri updater signing
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

          # macOS signing
          APPLE_CERTIFICATE: ${{ secrets.MAC_CERT }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.MAC_CERT_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

        with:
          projectPath: client
          tauriScript: npx tauri
          releaseId: ${{ needs.create-release.outputs.release_id }}
          tagName: v${{ needs.create-release.outputs.version }}
          releaseName: "v${{ needs.create-release.outputs.version }}"
          releaseBody: "Automated desktop build for Angular Momentum"
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}


  build-mobile:
    if: true
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.14.0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios,aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Install dependencies
        run: |
          cd client
          npm ci

      - name: Import iOS certificates
        env:
          IOS_CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE }}
          IOS_CERTIFICATE_PASSWORD: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
          IOS_PROVISION_PROFILE_BASE64: ${{ secrets.IOS_MOBILE_PROVISION }}
        run: |
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          KEYCHAIN_PASSWORD=$(uuidgen)
          
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          echo "$IOS_CERTIFICATE_BASE64" | base64 --decode > certificate.p12
          security import certificate.p12 -k "$KEYCHAIN_PATH" -P "$IOS_CERTIFICATE_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "$IOS_PROVISION_PROFILE_BASE64" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/profile.mobileprovision

          security list-keychains -d user -s "$KEYCHAIN_PATH"
          security default-keychain -d user -s "$KEYCHAIN_PATH"
          
          rm certificate.p12

      - name: Build iOS
        run: |
          cd client
          npm run tauri ios build -- --export-method app-store-connect
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Find iOS IPA path
        id: find-ipa
        run: |
          IPA_PATH=$(find client/src-tauri/gen/apple/build -name "*.ipa" -type f | head -n 1)
          echo "ipa_path=$IPA_PATH" >> $GITHUB_OUTPUT
          echo "Found IPA at: $IPA_PATH"

      - name: Upload iOS IPA (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: ${{ steps.find-ipa.outputs.ipa_path }}

      - name: Install Fastlane
        run: gem install fastlane

      - name: Upload to TestFlight
        uses: apple-actions/upload-testflight-build@v1
        with:
          app-path: ${{ steps.find-ipa.outputs.ipa_path }}
          issuer-id: ${{ secrets.APPSTORE_ISSUER_ID }}
          api-key-id: ${{ secrets.APPSTORE_API_KEY_ID }}
          api-private-key: ${{ secrets.APPSTORE_API_PRIVATE_KEY }}

      - name: Upload & Submit to App Store via Fastlane
        run: |
          fastlane deliver \
            --ipa "${{ steps.find-ipa.outputs.ipa_path }}" \
            --skip_screenshots \
            --skip_metadata \
            --submit_for_review \
            --automatic_release \
            --force
        env:
          APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APPSTORE_API_KEY_ID }}
          APP_STORE_CONNECT_API_KEY_KEY: ${{ secrets.APPSTORE_API_PRIVATE_KEY }}

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          yes | sdkmanager --licenses
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r26b
          local-cache: true
      - name: Set Android env variables
        run: |
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "NDK_HOME=$ANDROID_NDK_HOME" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$ANDROID_NDK_HOME" >> $GITHUB_ENV
          echo "PATH=$ANDROID_SDK_ROOT/platform-tools:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_NDK_HOME:$PATH" >> $GITHUB_ENV

      - name: Create Android gradle.properties
        run: |
          mkdir -p client/src-tauri/gen/android
          FILE=client/src-tauri/gen/android/gradle.properties
          echo "org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8" > "$FILE"
          echo "android.useAndroidX=true" >> "$FILE"
          echo "kotlin.code.style=official" >> "$FILE"
          echo "android.nonTransitiveRClass=true" >> "$FILE"
          echo "android.nonFinalResIds=false" >> "$FILE"

      - name: Decode and setup release keystore
        run: |
          echo "${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}" | base64 --decode > release.keystore
          mkdir -p client/build-tools
          mkdir -p client/src-tauri/build-tools
          cp release.keystore client/build-tools/upload-keystore.jks
          cp release.keystore client/src-tauri/build-tools/upload-keystore.jks

      - name: Create keystore.properties for production
        run: |
          mkdir -p client/src-tauri/gen/android
          echo "password=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" > client/src-tauri/gen/android/keystore.properties
          echo "keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}" >> client/src-tauri/gen/android/keystore.properties
          echo "storeFile=../../../build-tools/upload-keystore.jks" >> client/src-tauri/gen/android/keystore.properties

      - name: Build Android
        run: |
          cd client
          npm run tauri android build

      - name: Upload Android AAB
        uses: actions/upload-artifact@v4
        with:
          name: android-build
          path: client/src-tauri/gen/android/app/build/outputs/bundle/*/release/*.aab

      - name: Upload Android APK (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: client/src-tauri/gen/android/app/build/outputs/apk/universal/release/app-universal-release.apk

      - name: Deploy to Google Play Store
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: app.angularmomentum
          releaseFiles: client/src-tauri/gen/android/app/build/outputs/bundle/universalRelease/app-universal-release.aab
          track: production  # Options: internal, alpha, beta, production
          status: completed
          inAppUpdatePriority: 2   # No effect without in-app update library

  deployment-summary:
    runs-on: ubuntu-latest
    needs: [deploy-web, create-release, build-and-release, build-mobile]
    if: always()  # Run even if some jobs fail or are skipped
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Get version
        id: version
        run: |
          VERSION=$(node -p "require('./client/package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Deployment Summary
        run: |
          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "📦 DEPLOYMENT SUMMARY - v${{ steps.version.outputs.version }}"
          echo "════════════════════════════════════════════════════════"
          echo ""
          echo "🌐 Web Application (Heroku):"
          echo "   Status: ${{ needs.deploy-web.result }}"
          echo ""
          echo "🖥️  Desktop Applications (GitHub Release):"
          echo "   Status: ${{ needs.build-and-release.result }}"
          if [[ "${{ needs.build-and-release.result }}" == "success" ]]; then
            echo "   ✅ Windows, macOS, and Linux builds available"
            echo "   📥 Download: https://github.com/${{ github.repository }}/releases/latest"
          fi
          echo ""
          echo "📱 Mobile Applications:"
          echo "   Overall Status: ${{ needs.build-mobile.result }}"
          echo ""
          if [[ "${{ needs.build-mobile.result }}" == "success" ]]; then
            echo "   🍎 iOS (App Store):"
            echo "      ✅ Uploaded to TestFlight"
            echo "      ✅ Submitted for App Store Review"
            echo "      ⏳ Awaiting Apple approval (1-7 days typical)"
            echo "      📱 Manage: https://appstoreconnect.apple.com"
            echo ""
            echo "   🤖 Android (Google Play):"
            echo "      ✅ Uploaded to Google Play Console"
            echo "      ✅ Published to Production track"
            echo "      ⏳ Awaiting Google review (1-3 days typical)"
            echo "      📱 Manage: https://play.google.com/console"
            echo ""
            echo "   📦 Build Artifacts:"
            echo "      https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          elif [[ "${{ needs.build-mobile.result }}" == "failure" ]]; then
            echo "   ❌ Mobile build failed - check logs above"
          elif [[ "${{ needs.build-mobile.result }}" == "skipped" ]]; then
            echo "   ⏭️  Mobile builds skipped (job disabled)"
          fi
          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "🔗 Useful Links:"
          echo "════════════════════════════════════════════════════════"
          echo "   • This workflow run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "   • App Store Connect: https://appstoreconnect.apple.com"
          echo "   • Google Play Console: https://play.google.com/console"
          echo "   • Latest Release: https://github.com/${{ github.repository }}/releases/latest"
          echo ""
          
          # Overall status emoji
          if [[ "${{ needs.build-mobile.result }}" == "success" ]]; then
            echo "✅ Deployment pipeline completed successfully!"
          elif [[ "${{ needs.build-mobile.result }}" == "failure" ]]; then
            echo "⚠️  Some deployments failed - check details above"
            exit 1
          else
            echo "ℹ️  Deployment pipeline completed with some jobs skipped"
          fi
